---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
const recentPosts = posts.sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime()).slice(0, 3);
---

<BaseLayout title="jeansy.org">
  <Fragment slot="head">
    <!-- JSON-LD Structured Data for Person -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Richard Jeans",
      "url": "https://jeansy.org",
      "sameAs": [
        "https://github.com/rjeans",
        "https://www.linkedin.com/in/rjeans/",
        "https://x.com/rjeans",
        "https://www.instagram.com/rickj66/",
        "https://www.threads.com/@rickj66"
      ],
      "description": "Personal website, sharing thoughts on projects, ideas, and things that spark my curiosity."
    })} />

    <!-- JSON-LD Structured Data for Website -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "jeansy.org",
      "url": "https://jeansy.org",
      "author": {
        "@type": "Person",
        "name": "Richard Jeans"
      },
      "description": "Personal website, sharing thoughts on projects, ideas, and things that spark my curiosity.",
      "inLanguage": "en-US"
    })} />
  </Fragment>
  <div>
    <div class="mb-16">
      <h1 class="text-2xl font-bold mb-4" style="color: var(--text-primary);">Richard Jeans</h1>
      <p class="text-lg" style="color: var(--text-secondary); line-height: 1.8;">
        Personal website, sharing thoughts on projects, ideas, and things that spark my curiosity. All opinions mine. For my professional life, please get in touch via <a href="https://www.linkedin.com/in/rjeans/" target="_blank" rel="noopener noreferrer" style="color: var(--link-color); text-decoration: none;" class="hover:underline">LinkedIn</a>.
      </p>
    </div>

    <div>
      <h2 class="text-xl font-bold mb-6" style="color: var(--text-primary);">Recent Posts</h2>
      
      {recentPosts.length === 0 ? (
        <p style="color: var(--text-secondary);">No blog posts yet. Check back soon!</p>
      ) : (
        <div class="space-y-8">
          {recentPosts.map((post) => (
            <article class="border-b pb-6" style="border-color: var(--border-color);">
              <div class="text-sm mb-2" style="color: var(--text-muted);">
                {new Date(post.data.pubDate).toLocaleDateString('en-GB', {
                  year: 'numeric',
                  month: '2-digit',
                  day: '2-digit'
                })}
              </div>
              <h3 class="text-xl font-semibold mb-2">
                <a href={`/blog/${post.slug}`} style="color: var(--link-color); text-decoration: none;" class="hover:underline">
                  {post.data.title}
                </a>
              </h3>
              <p style="color: var(--text-secondary); line-height: 1.6;">
                {post.data.description}
              </p>
            </article>
          ))}
        </div>
      )}

      <div class="mt-8">
        <a href="/blog" style="color: var(--link-color); text-decoration: none;" class="hover:underline">
          View all posts
        </a>
      </div>
    </div>
  </div>
</BaseLayout>